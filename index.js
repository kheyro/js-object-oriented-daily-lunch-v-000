let store = { deliveries: [], meals: [], employers: [], customers: []}
let deliveryId = 0, customerId = 0

class Customer {
  constructor(name, employer) {
    this.name = name
    this.employer = emloyer.id
  }
}
